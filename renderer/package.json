{
  "name": "refworld-spark-renderer",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check app.mjs && node --check capture.mjs && node --test test_projection.mjs",
    "capture": "node capture.mjs"
  },
  "dependencies": {
    "@sparkjsdev/spark": "2.1.0",
    "three": "0.180.0"
  },
  "devDependencies": {
    "playwright": "1.62.1"
  }
}
